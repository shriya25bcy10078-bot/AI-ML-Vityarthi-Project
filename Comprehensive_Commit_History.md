Initially, for setting up git and github, following commands for used :-

git config -global user.name "shriya25bcy10078-bot"
git config -global user.email "shriya.25bcy10078@vitbhopal.ac.in"
git config --list
To add files in in project :- "git add ." command is used. It add all the files to git and github.

To commit files following commands are used :-

1. git commit -m "expense_tracker.py"
2. git commit -m "Update expense_tracker.py"
3. git commit -m "expense.py"
4. git commit -m "Update expense.py"
5. git commit -m "expense.csv"
6. git commit -m "Update expense.csv"
7. git commit -m "README.md"
8. git commit -m "Update README.md"
9. git commit -m "statement.md"
10. git commit -m "Update statement.md"
11. git commit -m "Comprehensive_Commit_History.md"
12. git commit -m "Update Comprehensive_Commit_History.md"
13. To push files into github repository, following command is used :- git push origin main

<img width="915" height="530" alt="Screenshot 2025-11-26 013121" src="https://github.com/user-attachments/assets/c95b1646-99b7-4abc-a508-dae2c44b0ea1" />
